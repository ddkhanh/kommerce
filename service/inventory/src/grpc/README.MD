Do not modify any files in here, they all generated follow protobuf schema

Change the schema in [protobuf](../protobuf) instead.

After that execute this command to generate ts file

```bash
yarn codegen
```